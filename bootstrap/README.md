# Liquid IDE ARM64 bootstrap

`bootstrap-aarch64.zip` is generated from the ARM64 Debian packages in this repository. It installs the Liquid runtime prefix under `/data/data/com.liquidide/files/usr`.
